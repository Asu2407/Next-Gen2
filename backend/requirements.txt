# SAHAYAK — Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework & server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Request/response models
pydantic>=2.6.0

# Multipart form parsing — required for File/UploadFile in routes/intake.py
python-multipart>=0.0.9

# Outbound HTTP calls to OpenAI Whisper (STT) & GPT (llm_extractor) APIs
httpx>=0.27.0
